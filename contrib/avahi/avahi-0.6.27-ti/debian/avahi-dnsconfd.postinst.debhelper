# Automatically added by dh_installinit
if [ -e "/etc/init/avahi-dnsconfd.conf" ]; then
	# start fails if already running
	start avahi-dnsconfd || :
fi
# End automatically added section
# Automatically added by dh_installinit
update-rc.d -f avahi-dnsconfd remove >/dev/null || exit $?
# End automatically added section
